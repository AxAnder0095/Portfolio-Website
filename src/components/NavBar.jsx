import { NavLink, Link } from "react-router-dom";
import '../styles/NavBar.scss'

export const Navbar = () => {
    return (
        <div>
            <nav>
                
            </nav>
        </div>
    )
};